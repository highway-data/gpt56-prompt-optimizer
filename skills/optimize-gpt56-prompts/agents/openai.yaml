interface:
  display_name: "GPT-5.6 Prompt Optimizer"
  short_description: "Reshape prompts for efficient, reliable GPT-5.6 work"
  default_prompt: "Use $optimize-gpt56-prompts to rewrite this prompt for GPT-5.6 while preserving my intent and authorization boundaries."
